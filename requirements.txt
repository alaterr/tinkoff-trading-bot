# PyPI package is currently quarantined/unavailable.
# Install directly from the official GitHub repository.
# For fully reproducible builds you can pin a commit SHA later.
tinkoff-investments @ git+https://github.com/Tinkoff/invest-python.git@main
pydantic[dotenv]==1.9.1
numpy==1.22.4
pytest==7.1.2
pytest-mock==3.7.0
pytest-asyncio==0.18.3