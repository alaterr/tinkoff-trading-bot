apiVersion: v1
kind: Secret
metadata:
  name: tinkoff-bot-secrets
  namespace: tinkoff-bot
type: Opaque
stringData:
  # Optional in job mode (token can be set via UI at runtime).
  # Strongly recommended to keep empty and set token only via kubectl port-forward.
  TOKEN: ""
  ACCOUNT_ID: ""

